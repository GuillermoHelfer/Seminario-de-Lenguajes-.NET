using System.Text;

Console.WriteLine("ingrese la frase a encriptar");
String frase = Console.ReadLine();

StringBuilder key = new StringBuilder();
Console.WriteLine("ingrese la clave de cifrado");
key.Append(Console.ReadLine());
Queue<char> q = new Queue<char>();
for(int i=0; i<key.Length; i++){
    q.Enqueue()
}

Dictionary<char,int> dct = new Dictionary<char,int>();

for (int i=1, j= 65; i< 28; i++, j++)
    dct.Add((char)j, i);
dct.Add((char)32, 28);

StringBuilder cifrar (string frase, Queue<char> kq, Dictionary<char,int> dct)
{
    for (int i=0; i<=28; i++)
    {
        
    }
    return null;
}

